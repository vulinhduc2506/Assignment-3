spring.application.name=ticket_management
# TODO [MENTOR REVIEW]: Schema được tạo bằng SQL riêng thì ứng dụng nên dùng validate để phát hiện
# entity ánh xạ sai cột/kiểu dữ liệu ngay khi khởi động; ddl-auto=none đang che mất lỗi mapping.
spring.jpa.hibernate.ddl-auto=none
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
